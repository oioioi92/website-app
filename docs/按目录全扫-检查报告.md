# 按目录全扫 — 检查报告

对 `app/`、`components/`、`lib/`、`config/`、`scripts/`、`services/chat-server/src/` 做逐目录扫描后的结论与建议。

---

## 1. app/

### 1.1 页面与路由
- **admin/login**、**(secure)/***：正常，后台鉴权由 layout 与 API 统一处理。
- **api/admin/***：均已使用 `getAdminUserFromRequest`，无未鉴权管理接口。
- **api/public/***：部分使用 `ensureInternalTestMode()`（INTERNAL_TEST_MODE），符合预期。
- **promotionFontPreset/page.tsx**、**promotionPattern/page.tsx**：仅做 `redirect("/")`，属占位/别名页，可保留或后续统一清理。

### 1.2 死代码（已删除）
- **app/api/admin/chat/ip-location/route.ts**：与 **api/admin/chat/geo/route.ts** 功能重复（均为 ip-api.com 查 IP 位置），前台仅调用 `geo`。已删除 **ip-location** 整目录。

### 1.3 内部接口
- **api/internal/ledger/commit**、**api/internal/provider-events/ingest**：当前返回 NOT_IMPLEMENTED，未暴露敏感逻辑；日后实现时需用内部密钥或 IP 白名单保护。

---

## 2. components/

### 2.1 死组件（已删除）
- **AdminChatQueueClient.tsx**：仅导出，无任何引用。后台聊天页使用 `AdminLiveChatClient`。已删除。
- **ChatFab.tsx**：仅导出，无引用。前台使用 `LiveChatFab`。已删除。

### 2.2 可能未使用（建议人工确认）
- **BlocksRenderer.tsx** + **blocks.ts**：仅彼此引用，无其他文件 import。若未来不做「区块配置 + 统一渲染」可考虑删；若预留则保留。

### 2.3 其余
- **LiveChatFab**、**EmbeddedChatClient**：被 MobileShell、chat 页使用，正常。
- **DesktopHeader**、**ReportsAndPromos**：被 HomeCoverClient 使用，正常。

---

## 3. lib/

### 3.1 未引用（建议人工确认）
- **lib/security/crypto.ts**：提供 `encryptString` / `decryptString`（TOTP 等加密），当前无任何 import。若为预留 TOTP 可保留，否则可删。
- **lib/backoffice/metrics-dictionary.ts**：导出 CASH_IN、CASH_OUT 等常量，无任何 import。若报表口径未用可删。

### 3.2 重复路径（仅路径写法不同，非重复文件）
- `lib/internal-test.ts` 与 `lib\internal-test.ts` 等：同一文件，Windows 下正反斜杠差异，无需处理。

### 3.3 其余
- **lib/crypto.ts**：被 auth、csrf、backoffice/ids 使用。
- **ensureInternalTestMode**：被多个 public API 使用。
- **report-center-config**、**report-api-types**、**filter-options**、**pagination**、**ids**：均有引用。

---

## 4. config/
- **admin-nav.ts**、**reportCatalog.config.ts**、**transactions.presets.ts**：被引用，无异常。

---

## 5. scripts/
- 多为一次性或 CI 脚本，**console.log** 为脚本输出，保留合理。
- **debug-***、**smoke-***、**seed-***、**import-*** 等：用途明确，未发现多余依赖或死代码。

---

## 6. services/chat-server/src/
- **index.ts**：启动时若干 `console.log`（SERVER_OK、DB_OK 等）为进程就绪日志，保留。
- 无新增调试 ingest 或未使用导出。

---

## 7. 已执行删除汇总
| 类型 | 路径 | 原因 |
|------|------|------|
| API 路由 | `app/api/admin/chat/ip-location/` | 与 `geo` 重复，且未被调用 |
| 组件 | `components/admin/AdminChatQueueClient.tsx` | 无引用，后台聊天已用 AdminLiveChatClient |
| 组件 | `components/public/ChatFab.tsx` | 无引用，前台已用 LiveChatFab |

---

## 8. 建议人工确认后处理
| 路径 | 说明 |
|------|------|
| `components/BlocksRenderer.tsx` + `components/blocks.ts` | 仅互相引用，无其他使用；可删或预留 |
| `lib/security/crypto.ts` | 仅定义 encrypt/decrypt，无引用；TOTP 预留可保留 |
| `lib/backoffice/metrics-dictionary.ts` | 无引用；若报表不用可删 |

---

*报告生成自按目录全扫，后续若有新页面/组件可再跑一轮引用检查。*
