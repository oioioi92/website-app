# 后台检查报告

> 全量检查侧栏、Settings、报表、API 与占位项。最后更新：与当前代码一致。

---

## 一、已确认正常（无需修复）

### 侧栏入口（admin-nav.ts）
- OPS：Live Chat、WhatsApp 收件箱、待发送账号、Pending Deposit/Withdraw、Transfer Queue → 均有对应 page，无 404。
- REPORT：Daily Sales(hourly-sales)、Game Sales(winloss-by-game)、Bonus(bonus→bonus-cost)、Bank(bank→reconciliation)、All Reports → slug 与 API 映射正确。
- HISTORY：Transactions、Gateway Search、Reconciliation → 有 page 且 API 映射正确。
- CONTENT：Promotions、图片转网址 → 已接通。
- USERS：Players、Agents、Referral Tree、Admin Accounts → 已接通。
- SYSTEM：Settings → 已接通。

### Settings（settings-nav.ts）
- Theme、Frontend、Promotion List、Referral、Finance（4 项）、Integrations（2 项）、Account & Security（5 项）→ 所有 href 均有 page 或 redirect，且功能已连接（Theme/Frontend 用 ThemeSettingsClient，Referral 用 AdminReferralSettingsClient，Deposit Rules 用 DepositTopupRulesSettingsClient 等）。

### 重定向与合并入口
- `/admin/security` → `/admin/settings/account/security`
- `/admin/settings/referral` → `/admin/settings/referral/general`
- `/admin/settings/login-history` → `/admin/settings/account/login-history`
- Finance：banks→bank、deposit-rules→deposit-topup-rules、payment-gateways→payment-gateway
- Integrations：whatsapp→settings/whatsapp；游戏管理直链 game-providers
- game-api、games → game-providers

### 报表中心
- REPORT_CENTER_SLUGS 含 bonus、bank；SLUG_TO_REPORT_KEY["bonus"]=bonus-cost、"bank"=reconciliation。
- 未知 slug 回退到 AdminReportView，不 404。

### API 与表单
- Theme：GET/PATCH /api/admin/theme，ThemeSettingsClient 读写正常。
- Referral：GET/PUT /api/admin/settings/referral，AdminReferralSettingsClient 正常。
- Deposit/Topup Rules：GET/PUT /api/admin/settings/deposit-topup-rules，DepositTopupRulesSettingsClient 正常。
- Bank、Payment Gateway、WhatsApp、Game Providers 等均有对应 API 与客户端。

---

## 二、可选改进（非必须）

| 项 | 说明 |
|----|------|
| **双 URL 统一** | 已做：`/admin/settings/profile`、`/admin/settings/password` 已 redirect 至 `/admin/settings/account/profile`、`/admin/settings/account/password`。 |
| **Sticky Save Bar 统一** | 各设置页保存条与成功/失败提示样式可统一成一套组件（体验优化）。 |
| **占位报表接真实 API** | Customer、Promotion Report、Commission 等仍用 ReportPlaceholder；若后端有接口可接 ReportTableFromApi。 |

---

## 三、已知限制（占位/规划中）

| 项 | 说明 |
|----|------|
| **玩家详情部分 Tab** | 玩家详情弹窗中 Bet、Commission、Credit、Setting、Problem、Game、IP、Similarity、User Tag、Log 等 Tab 显示「comingSoon」，属后续功能规划。 |
| **部分报表占位** | 报表中心内部分 slug（如多张占位报表）使用 ReportPlaceholder，标题与「即将推出」由 admin.reportPlaceholder.* 与 admin.reportTitle.* 控制。 |
| **turnover-by-game** | 当前为说明 + 跳转至 winloss-by-game，非独立报表。 |

---

## 四、结论

- **无 404、无断链**：侧栏与 Settings 所有入口均有对应页面或重定向，且关键页均接好 API。
- **可修复项**：无必须修复项；仅有上述可选改进与已知占位，可按排期处理。
