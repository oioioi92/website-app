# 后台全量修复 — 回归验收清单

本文档对应「后台全量修复计划」执行后的桌面端与移动端回归检查项。请在部署后按此清单逐项验证。

## P0：核心可用性与安全

### 游戏管理
- [ ] **Settings → 游戏供应商**：列表加载正常，名称/代码/Logo/启用可编辑，保存成功
- [ ] **游戏管理（fullManagement）**：新建游戏、排序（上/下）、启用开关、Logo 上传均可用；无字段重叠、按钮挤压
- [ ] 翻译键均正常显示（无 `admin.gamesManagement.xxx` 等裸 key）

### Domain
- [ ] **Admin → Domain**：允许的域名列表加载/添加/移除正常
- [ ] **空列表可保存**：清空所有域名后点击保存，接口返回成功且再次进入页面仍为空列表

### Tools（图片转网址）
- [ ] **Admin → Tools**：上传图片获得链接功能正常
- [ ] 文案为「建议图片宽度不超过 1500px…」，无误导性「Max Width: 1500 px」强制限制

### Security 权限
- [ ] **ip-whitelist / login-history / activity-log**：仅 admin 角色可访问；非 admin 请求返回 403
- [ ] **/admin/security** 访问时重定向到 **/admin/settings/account/security**，功能与 Security 页一致

## P1：报表与设置

### 报表
- [ ] 筛选条件与后端一致，点击「搜索」后表格与汇总正确
- [ ] **分页**：当 `total_count > pageSize` 时显示分页控件，上一页/下一页、页码与总数正确
- [ ] **CSV 导出**：按钮点击后显示「导出中…」，成功显示「导出成功」，失败显示错误提示

### Settings 信息架构
- [ ] **Settings 首页**：六大块入口正确，点击进入对应子页
- [ ] **Settings 侧栏**：使用 `group.children` 渲染，无 `group.items` 报错；当前路径高亮正确
- [ ] **Security**：统一入口为 **/admin/settings/account/security**，2FA/白名单等在此页配置

## P2：Theme 与前台联动

### 主题
- [ ] **Theme 设置**：整页背景图（pageBackgroundUrl）、整页背景色（pageBackgroundColor）可编辑并保存
- [ ] 保存后 **invalidateThemeCache** 已调用，前台刷新后背景/按钮/图片改动即时生效

## 桌面端与移动端

### 桌面端
- [ ] 上述所有后台页面在桌面浏览器下布局正常、无错位
- [ ] 报表表格横向滚动、分页与导出在桌面端可用

### 移动端（或窄屏）
- [ ] 后台在窄屏下侧栏/表格可正常使用（或可横向滚动）
- [ ] 游戏管理卡片、Domain 列表、Theme 表单项在移动端可操作

## 未通过项记录

若某项未通过，请在此处记录：

| 检查项 | 结果 | 备注 |
|--------|------|------|
| （示例）Domain 空列表保存 | 失败 | 接口返回 400 |
| | | |

---

*生成自「后台全量修复计划」执行结果。*
