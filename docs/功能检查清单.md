# 后台功能检查清单

> 最后更新：按对话总结与本次修复整理。用于确认「无漏接、无 404」。

## 一、侧栏入口与路由（config/admin-nav.ts）

| 入口 | href | 状态 | 说明 |
|------|------|------|------|
| Live Chat | /admin/chat | 已接通 | 聊天队列 |
| WhatsApp 收件箱 | /admin/whatsapp-inbox | 已接通 | 商业号会话 |
| 待发送账号 | /admin/register-pending | 已接通 | 新注册待发 ID/密码 |
| Pending Deposit | /admin/deposits/pending | 已接通 | 待审核入款 |
| Pending Withdraw | /admin/withdrawals/pending | 已接通 | 待处理提款 |
| Transfer Queue | /admin/transfers | 已接通 | 转分排队 |
| Daily Sales | /admin/reports/hourly-sales | 已接通 | 报表 API hourly-sales |
| Game Sales | /admin/reports/winloss-by-game | 已接通 | 报表 API winloss-by-game |
| Bonus | /admin/reports/bonus | 已修复 | slug 映射 bonus-cost，走 ReportTableFromApi |
| Bank Transaction | /admin/reports/bank | 已修复 | slug 映射 reconciliation，走 ReportTableFromApi |
| All Reports | /admin/reports | 已接通 | 报表中心首页 |
| Transactions | /admin/transactions | 已接通 | 统一流水 |
| Gateway Search | /admin/reports/gateway-search | 已接通 | 报表 API gateway-search |
| Reconciliation | /admin/reports/reconciliation | 已接通 | 报表 API reconciliation |
| Promotions | /admin/promotions | 已接通 | 优惠列表；空状态已 i18n |
| Players | /admin/players | 已接通 | 玩家列表 |
| Agents | /admin/agents | 已接通 | 代理列表 |
| Referral Tree | /admin/referrals | 已接通 | 推荐树 API + 树形组件 |
| Settings | /admin/settings | 已接通 | 设置总览 |

## 二、报表 slug 与 API 映射（report-center-config.ts）

- bonus -> reportKey bonus-cost（红利成本汇总）
- bank -> reportKey reconciliation（对账）
- bonus-cost-summary -> bonus-cost
- 其余 slug 与 reportKey 一致或走 ledger-transactions

## 三、本次修复项

1. /admin/reports/bonus：REPORT_CENTER_SLUGS 增加 bonus，SLUG_TO_REPORT_KEY["bonus"]=bonus-cost，三语 reportTitle 与 SLUG_TO_TITLE 已补。
2. /admin/reports/bank：REPORT_CENTER_SLUGS 增加 bank，SLUG_TO_REPORT_KEY["bank"]=reconciliation，三语 reportTitle 与 SLUG_TO_TITLE 已补。
3. Promotions 空状态：两处「暂无优惠活动」改为 t("admin.promotionsList.noPromotions")，ZH/EN/MS 已补 noPromotions。

## 四、报表中心 REPORT_CENTER_SLUGS 覆盖

以下 slug 均走 ReportTableFromApi 或专项组件：all-transactions, deposit-report, withdraw-report, hourly-sales, winloss-by-game, winloss-by-player, bonus, bonus-transactions, bonus-cost, bonus-cost-summary, transfer-report, gateway-search, reconciliation, bank, user-kpi。特殊：turnover-by-game 占位+跳转；transactions-detail、daily-summary 用专用组件。

## 五、建议后续自检

浏览器逐一点击侧栏与报表中心各链接，确认无 404、无空白页。新增 nav 或报表 slug 时同步更新 REPORT_CENTER_SLUGS、SLUG_TO_REPORT_KEY 及 i18n。
