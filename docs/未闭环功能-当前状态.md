# 未闭环功能 — 当前状态

根据代码与配置核对后的实际状态（供验收与排期参考）。

---

## 一、报表相关

| 项目 | 状态 | 说明 |
|------|------|------|
| **Transaction Report 的 Type 筛选** | 已接后端 | 前端传 `type`（All / Deposit / Withdraw），API `getTransactionReport` 读 `sp.get("type")` 并按类型过滤存款/提款。 |
| **Transaction Report 导出 CSV** | 已做 | 报表 API 支持 `format=csv`，Transaction Report 页有「Export CSV」按钮，请求带 `format=csv` 并触发下载。 |
| **多张报表为占位页** | 已接 | bank、gateway-search、winloss-by-player、top-referrer、promotion-report、commission、staff 等已加入 PLACEHOLDER_REPORT_SLUGS，点击显示「即将推出」；Customer 仍映射 user-kpi 有数据。 |

---

## 二、Settings 结构

| 项目 | 状态 | 说明 |
|------|------|------|
| **Frontend 入口** | 已精简 | 导航仅保留单入口「Frontend」→ home-media（ThemeSettingsClient）；其余子路径仍可访问，同源保存。 |
| **Settings 二级导航** | 已有 | settings/layout 左侧使用 SETTINGS_NAV（THEME、Frontend、Promotion Center、Referral、Finance、Integrations、Account）。 |
| **Account & Security 扩展** | 已做 | 2FA（Security 页 TwoFactorSettingsClient）、Login History（独立页）、Admin Accounts（创建 admin/editor/viewer）、Security 页含 IP 白名单与「Create Admin」入口；Login History 仅保留独立子页，Security 页不再重复展示。 |
| **Withdraw Rules** | 已做 | Settings → Finance → Withdraw Rules 已接 GET/PUT /api/admin/settings/withdraw-rules，可配置启用、单笔最低/最高金额、每日次数上限。 |

---

## 三、其它能力

| 项目 | 状态 | 说明 |
|------|------|------|
| **Game API / Payment Gateway / WhatsApp 测试** | 已做 | 三处均有「测试连接」按钮并调用对应 /test API，展示成功/失败信息。 |
| **Deposit / Topup Rules** | 已做 | DepositTopupRulesSettingsClient 接 GET/PUT /api/admin/settings/deposit-topup-rules，开关与 maxBalanceForTopup 可保存。 |
| **Sticky Save Bar 统一** | 已做 | 各设置页已统一 StickySaveBar 与 i18n 提示。 |

---

## 四、本次已接上

1. **Withdraw Rules**：Settings → Finance → Withdraw Rules 已接 GET/PUT /api/admin/settings/withdraw-rules，可配置启用、单笔最低/最高金额、每日次数上限，WithdrawRulesSettingsClient + StickySaveBar。
2. **报表占位**：bank、gateway-search、winloss-by-player、top-referrer、promotion-report、commission、staff 等 slug 已加入 PLACEHOLDER_REPORT_SLUGS，点击显示「即将推出」。
3. **玩家详情 Tab**：Tab 文案已接 i18n（admin.players.tabXxx），Credit Tab 展示主钱包余额，其余 Tab 统一「该功能后续开放」，关闭按钮 i18n。

## 五、建议后续优先

1. 占位报表接真实数据（如 Top Referrer、Commission、Bank 对账）。
2. Deposit Rules 完整化（可选）。
