# 未闭环功能 — 当前状态

根据代码与配置核对后的实际状态（供验收与排期参考）。

---

## 一、报表相关

| 项目 | 状态 | 说明 |
|------|------|------|
| **Transaction Report 的 Type 筛选** | 已接后端 | 前端传 `type`（All / Deposit / Withdraw），API `getTransactionReport` 读 `sp.get("type")` 并按类型过滤存款/提款。 |
| **Transaction Report 导出 CSV** | 已做 | 报表 API 支持 `format=csv`，Transaction Report 页有「Export CSV」按钮，请求带 `format=csv` 并触发下载。 |
| **多张报表为占位页** | 占位 | Customer、Promotion、Commission 等使用 ReportPlaceholder，暂无真实接口。 |

---

## 二、Settings 结构

| 项目 | 状态 | 说明 |
|------|------|------|
| **Frontend 六子页** | 路由已拆 | General、Notices、Popups、Download Bar、Home Media、Display 均有路由，部分为占位或跳转。 |
| **Settings 二级导航** | 已有 | settings/layout 左侧使用 SETTINGS_NAV（THEME、Frontend、Promotion Center、Referral、Finance、Integrations、Account）。 |
| **Account & Security 扩展** | 已做 | 2FA（Security 页 TwoFactorSettingsClient）、Login History（独立页）、Admin Accounts（创建 admin/editor/viewer）、Security 页含 IP 白名单与「Create Admin」入口；Login History 仅保留独立子页，Security 页不再重复展示。 |

---

## 三、其它能力

| 项目 | 状态 | 说明 |
|------|------|------|
| **Game API / Payment Gateway / WhatsApp 测试** | 已做 | 三处均有「测试连接」按钮并调用对应 /test API，展示成功/失败信息。 |
| **Deposit Rules 完整化** | 未做 | 规则与交互仍有 Coming soon 或未完全实现。 |
| **Sticky Save Bar 统一** | 未统一 | 各设置页的 Save、成功/失败提示尚未统一成一套规范。 |

---

## 四、建议后续优先

1. 占位报表接真实数据（如 Customer、Top Referrer）。
2. Deposit Rules 完整化。
3. Sticky Save Bar / 成功失败提示统一（可选）。
