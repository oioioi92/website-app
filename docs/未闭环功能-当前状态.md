# 未闭环功能 — 当前状态

根据代码与配置核对后的实际状态（供验收与排期参考）。

---

## 一、报表相关

| 项目 | 状态 | 说明 |
|------|------|------|
| **Transaction Report 的 Type 筛选** | ✅ 已接后端 | 前端传 `type`（All / Deposit / Withdraw），API `getTransactionReport` 读 `sp.get("type")` 并按类型过滤存款/提款。 |
| **Transaction Report 导出 CSV** | ✅ 已做 | 报表 API 支持 `format=csv`，Transaction Report 页有「Export CSV」按钮，请求带 `format=csv` 并触发下载。 |
| **多张报表为占位页** | ✅ 已全部接入 | 所有报表 slug 均已对接 API：Transaction、Customer、Top Referrer、Promotion、Commission、Staff、Activity Log、Rebate/Angpao、Manual（LedgerTx）、Leaderboard（真实数据）、Feedback/ReferrerClick/Lucky Number/Lucky Draw 4D（空表结构，待业务数据）。 |

---

## 二、Settings 结构

| 项目 | 状态 | 说明 |
|------|------|------|
| **Frontend 六子页** | ✅ 路由已拆 | General、Notices、Popups、Download Bar、Home Media、Display 均有路由（`/admin/settings/frontend/*`），当前为占位+跳转 THEME/Site。 |
| **Settings 二级导航** | ✅ 已有 | `settings/layout.tsx` 左侧使用 `SETTINGS_NAV`（THEME、Frontend、Promotion Center、Referral、Finance、Integrations、Account）。 |
| **Account & Security 扩展** | 部分已做 | Login History、Activity Log、Theme 保存写审计已做；Security 页已加 2FA 说明（即将上线）；侧栏按 role 过滤（permission: admin）；2FA 完整流程按 P2 排期。 |

---

## 三、其它能力

| 项目 | 状态 | 说明 |
|------|------|------|
| **Game API / Payment Gateway / WhatsApp 测试** | ✅ 已做 | 三处均有「测试连接」按钮，调用 `/api/admin/settings/game-api/test`、`payment-gateway/test`、`whatsapp/test`，并展示成功/失败信息。 |
| **Deposit Rules 完整化** | ✅ 已做 | 规则 API + 表单（enabled、maxBalanceForTopup）+ StickySaveBar 已实现。 |
| **Banner / Partners 动态列表** | ✅ 已做 | THEME 设置页中 Hero Banners、Subsidiaries 已改为可增删、上移下移排序的列表，无条数上限。 |
| **Sticky Save Bar / 保存提示统一** | ✅ 已做 | StickySaveBar 已接入 Deposit Rules、Bank、THEME、Game API、Payment Gateway、WhatsApp 设置页。 |

---

## 四、建议后续优先

1. **占位报表接真实数据**：已全部接入。Feedback、ReferrerClick 已建表与提交/记录 API，报表读真实数据；Manual/Leaderboard 接真实数据；Lucky Number/Lucky Draw 4D 仍为预留空表。
2. **Account & Security 扩展**：Login/Activity Log/Theme 审计已做；侧栏按 role 显示（SETTINGS、TOOLS & SECURITY 仅 admin）；2FA 完整流程按 P2 排期。
3. **Banner/Partners 动态列表**：已完成（THEME 页 Hero Banners、Subsidiaries 可增删、排序）。

---

## 五、数据库变更（本次新增）

- **Feedback**、**ReferrerClick** 已加入 `schema.sqlite.prisma` 与 `schema.postgres.prisma`。部署前请执行：
  - `npx prisma generate --schema prisma/schema.sqlite.prisma`（或 postgres）
  - `npx prisma db push`（SQLite）或 `npx prisma migrate dev`（Postgres）以创建新表。

---

*文档根据当前代码与配置整理，有变更时请同步更新。*
