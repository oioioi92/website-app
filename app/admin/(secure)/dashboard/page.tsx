import { UnifiedReportDashboard } from "@/components/admin/UnifiedReportDashboard";

export const dynamic = "force-dynamic";

/** 统一报表 Dashboard：单页完成全部筛选与查看，可折叠筛选区、多报表区块联动 */
export default function AdminDashboardPage() {
  return <UnifiedReportDashboard />;
}
